# Minimal requirements for Falcon AI (CI/CD compatible)
# Core dependencies only - fallbacks are implemented for missing packages

# Essential packages
colorama>=0.4.4
aiohttp>=3.8.0
pyyaml>=5.4.1

# Optional but recommended
requests>=2.25.0